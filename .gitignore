# Secrets — NEVER commit these
.env
.env.*
*.gmail_oauth.json
.gmail_oauth.json

# Data volumes (bind-mounted from /data, not source)
cache
cache/
history/
snapshots/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Editor / OS noise
*.swp
.DS_Store

# v2 client build output — regenerate with `cd app && npm run build`
weather/v2/

# The brief archive is its own embedded git repo (scripts/archive_push.sh) —
# it must not churn the parent tree with a commit per day.
weather/brief/
